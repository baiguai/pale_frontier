#ifndef PLAYER_H
#define PLAYER_H

#include "../global.hpp"
#include <cmath>

namespace space_loop
{
    void loadPlayerTexture();
    void unloadPlayerTexture();
    void drawPlayer(float rotation);
}

#endif
